name: Repository hygiene

on:
  pull_request:
  push:

jobs:
  forbid-generated-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject debug images, archives, and temporary files
        run: bash scripts/check_repository_hygiene.sh
